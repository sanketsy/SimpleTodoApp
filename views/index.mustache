<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo App</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
</head>

<body class="alert-success">
    <div class="jumbotron bg-success text-light text-center">
        <h1 class="display-4">Stuff to do:</h1>
    </div>
    <div class="container">
        <div class="row">
            <form class="form d-flex" action="/todos" method="POST">
                <input class="form-control" placeholder="Enter task" type="text" name="desc" id="desc" />
                <input class="btn btn-success ml-2" type="submit" value="Add" id="btnAdd" />
            </form>
        </div>
        <div class="row mt-5">
            <ul>
                {{#todoData}}
                <li class="mt-2">
                    <div class="d-flex">
                        <p class="w-100 mr-3">{{desc}}</p>
                        <form class="ml-2" action="/todos/{{_id}}/completed" method="POST">
                            <input type="submit" class="btn btn-sm btn-outline-success" value="Done" />
                        </form>
                        <form class="ml-1" action="/todos/{{_id}}/deleted" method="POST">
                            <input type="submit" class="btn btn-sm btn-outline-danger" value="Delete" />
                        </form>
                    </div>
                </li>

                {{/todoData}}
            </ul>
        </div>
        <hr style="height:2px;background-color:rgb(0, 150, 0)" />
        <div class="row text-muted">
            <ul>
                {{#completedTodoData}}
                <li class="mt-2">
                    <div class="d-flex">
                        <p class="w-100 mr-3">{{desc}}</p>
                        <form class="ml-2" action="/todos/{{_id}}/completed" method="POST">
                            <input type="submit" class="btn btn-sm btn-outline-secondary" value="Whoopss" />
                        </form>
                        <form class="ml-1" action="/todos/{{_id}}/deleted" method="POST">
                            <input type="submit" class="btn btn-sm btn-outline-danger" value="Delete" />
                        </form>
                    </div>
                </li>
                {{/completedTodoData}}
            </ul>
        </div>
    </div>
</body>

</html>